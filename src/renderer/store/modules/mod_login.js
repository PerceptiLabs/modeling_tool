const namespaced = true;

const state = {
  showLoader: false
};

const mutations = {
  SET_showLoader (state, value) {
    state.showLoader = value
  },
  
};

const actions = {
 
};

export default {
  namespaced,
  state,
  mutations,
  actions
}
